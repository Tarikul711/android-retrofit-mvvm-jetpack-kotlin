Now I will discuss about the paging library in android.
-> Implement the file " implementation 'android.arch.paging:runtime:1.0.1' "
-> add the gson model class and config the api interface.
-> Change the recyclerview adapter. Extend 'PagedListAdapter' instead of RecyclerView.Adapter
-> Add DiffUtilCallBack class inside the adapter
->